server:
  port: 8000

spring:
  application:
    name: crowie.dev

  datasource:
    # Base de datos canónica: la crea database/schema.sql (ejecutar una vez con
    # `mysql -u root -p < database/schema.sql` desde la raíz del proyecto).
    url: jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:saas_clinica_odontologica}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    # El DDL lo posee database/schema.sql (ENUM, JSON, columna generada y
    # semillas). Hibernate NO crea ni altera tablas: 'none'.
    hibernate:
      ddl-auto: none
    # Esto imprimirá las consultas SQL en la consola, vital para debuggear el Nivel Senior
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true

  # Configuración para integración con Ollama local (Spring AI)
  ai:
    ollama:
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: ${OLLAMA_MODEL:qwen2.5-coder:7b}
